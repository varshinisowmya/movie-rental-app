spring.application.name=store
# Updated URL with the security fix
spring.datasource.url=jdbc:mysql://localhost:3306/movie_rental_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Your MySQL credentials (make sure password is correct!)
spring.datasource.username=root
spring.datasource.password=Performance@12345

# Keep these as they were
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true